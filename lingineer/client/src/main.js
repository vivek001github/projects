import { createApp } from 'vue'    // Import Vue 3
import App from './App.vue'         // Correct import for App.vue from the same directory

createApp(App).mount('#app')        // Mount the App component on an element with id 'app'
